# T066 实现导出门禁

## 完成内容

- 新增纯函数导出门禁，统一检查 manifest/入口/活动组合、安全诊断、Producer runtime、渲染状态和 alpha 输出。
- 返回结构化 blocker code、用户文案和修复动作，同时保留非阻塞 warning。
- 门禁不修改项目目录、时间线或缓存，失败后项目保持可编辑和可修复。

## 验证

- 1 个测试文件、2 个测试通过。
- 相关 4 个文件 lint 和类型检查通过，无警告。
